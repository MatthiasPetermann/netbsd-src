DIR /var/www/mysite-edge-httpd mode=0755 owner=root group=wheel

FILE_BEGIN /var/www/mysite-edge-httpd/index.html mode=0644
<!doctype html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>mysite-edge-httpd</title>
  <style>
    :root { color-scheme: light; }
    body {
      margin: 0;
      font-family: "Trebuchet MS", "Segoe UI", sans-serif;
      background: linear-gradient(150deg, #f7fbff, #ecf8f2);
      color: #1f2a33;
      min-height: 100vh;
      display: grid;
      place-items: center;
    }
    main {
      width: min(680px, 92vw);
      background: #ffffff;
      border: 1px solid #d6e5de;
      border-radius: 14px;
      padding: 1.25rem 1.4rem;
      box-shadow: 0 10px 28px rgba(20, 50, 38, 0.08);
    }
    h1 { margin: 0 0 .5rem; font-size: 1.35rem; }
    p { margin: .4rem 0; }
    code {
      background: #eef5f0;
      border-radius: 6px;
      padding: .15rem .35rem;
      font-size: .92em;
    }
  </style>
</head>
<body>
  <main>
    <h1>Willkommen aus mysite-edge-httpd</h1>
    <p>bozohttpd laeuft sauber in einer cell und antwortet auf <code>8080</code>.</p>
    <p>Diese Seite stammt direkt aus dem <code>cellmgr</code>-Beispielmanifest.</p>
  </main>
</body>
</html>
FILE_END
